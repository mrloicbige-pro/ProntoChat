# ProntoChat Client

This archive contains the Linux client commands `chat` and `chatd`.

## Install

```bash
./install.sh <username>
```

The installer handles Fedora dependencies, the user service, identity creation,
and the connection check. When updating an existing installation, the username
argument can be omitted:

```bash
./install.sh
```
